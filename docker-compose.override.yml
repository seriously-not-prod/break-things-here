# Local WSL2 override: removes bind mounts that fail in WSL2
# Only directory bind mounts fail in WSL2 — single-file mounts (like init.sql) work fine.
# The backend src dir bind mount is removed so the baked-in image files are used.
services:
  backend:
    volumes: !reset
      - uploads-data:/app/uploads
