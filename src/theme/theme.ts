import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6750a4',
    },
    secondary: {
      main: '#625b71',
    },
  },
  typography: {
    fontFamily: 'inherit',
  },
});

export default theme;
